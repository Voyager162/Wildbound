# In-game music

Place licensed `.mp3` tracks in this folder. While a playable world is open, Wildbound shuffles
these tracks and occasionally plays one without immediate repeats.

The per-second playback chance and minimum quiet interval are configured in
`src/audio/gameMusicConfig.ts`.

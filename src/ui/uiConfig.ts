// 1-100 minimap-area control: higher values show more world around the player.
// 50 keeps the current exploration scale.
export const MINIMAP_AREA_SCALE = 50;

// 1-100 minimap-border smoothness: higher values make biome borders smoother,
// lower values make them chunkier/crisper and reduce minimap redraw cost.
export const MINIMAP_BORDER_SMOOTHNESS_SCALE = 80;